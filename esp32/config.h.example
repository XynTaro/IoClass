#ifndef CONFIG_H
#define CONFIG_H

// WiFi credentials
#define WIFI_SSID "Okieee"
#define WIFI_PASS "11111111"

// Laravel API — use your PC's LAN IP, not localhost
#define SERVER_BASE "http://192.168.48.1:8000/api/rfid"

// Must match RFID_DEVICE_TOKEN in Laravel .env
#define DEVICE_TOKEN "esp32-room-a"

#endif
